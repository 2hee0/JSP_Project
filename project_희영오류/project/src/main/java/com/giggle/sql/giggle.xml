<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Giggle">
	<select id="LoginAction" parameterType="map" resultType="_int">
		SELECT count (*) FROM user_info WHERE user_id = #{user_id} AND user_pw = #{user_pw}
	</select>
	<insert id="JoinAciton" parameterType="giggledto">
    	INSERT INTO user_info values (#{user_id},#{user_pw},#{user_name},#{user_nick},#{user_gender},#{user_birth},#{user_phone}, #{membership_type}, #{membership_signup}, #{membership_expire})
	</insert>
</mapper>